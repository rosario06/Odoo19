<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vista List de Depreciación -->
    <record id="view_account_asset_depreciation_list" model="ir.ui.view">
        <field name="name">account.asset.depreciation.list</field>
        <field name="model">account.asset.depreciation</field>
        <field name="arch" type="xml">
            <list string="Líneas de Depreciación" create="0" delete="0">
                <field name="asset_id"/>
                <field name="depreciation_date"/>
                <field name="name"/>
                <field name="amount"/>
                <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-info="state == 'draft'" decoration-muted="state == 'cancelled'"/>
                <field name="move_id"/>
            </list>
        </field>
    </record>

    <!-- Vista Form de Depreciación -->
    <record id="view_account_asset_depreciation_form" model="ir.ui.view">
        <field name="name">account.asset.depreciation.form</field>
        <field name="model">account.asset.depreciation</field>
        <field name="arch" type="xml">
            <form string="Línea de Depreciación" create="0">
                <header>
                    <button name="action_post" string="Contabilizar" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_cancel" string="Cancelar" type="object" invisible="state != 'posted'"/>
                    <button name="action_draft" string="Volver a Borrador" type="object" invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="asset_id" readonly="1"/>
                            <field name="name"/>
                            <field name="depreciation_date"/>
                        </group>
                        <group>
                            <field name="currency_id" invisible="1"/>
                            <field name="amount" widget="monetary"/>
                            <field name="move_id" readonly="1"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción -->
    <record id="action_account_asset_depreciation" model="ir.actions.act_window">
        <field name="name">Depreciación</field>
        <field name="res_model">account.asset.depreciation</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>

