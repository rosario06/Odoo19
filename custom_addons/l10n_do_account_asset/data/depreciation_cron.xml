<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Cron Job para Depreciación Automática Mensual -->
    <record id="ir_cron_asset_depreciation" model="ir.cron">
        <field name="name">Activos Fijos: Depreciación Automática</field>
        <field name="model_id" ref="model_account_asset_depreciation"/>
        <field name="state">code</field>
        <field name="code">model._cron_generate_depreciation_entries()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">months</field>
        <field name="active" eval="False"/>
        <field name="priority">10</field>
    </record>

</odoo>

